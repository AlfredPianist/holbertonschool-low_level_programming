#include "holberton.h"

/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
int main(void)
{
	char *str;

	str = "";
	puts2(str);
	str = "0123456789a";
	puts2(str);
	str = "0123456789";
	puts2(str);
	return (0);
}
