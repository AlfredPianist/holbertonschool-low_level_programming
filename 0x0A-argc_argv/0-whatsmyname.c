#include "holberton.h"

/**
 * main - prints program name
 * @argc: Program's argument count.
 * @argv: Program's argument vector. Only 1: its name.
 *
 * Return: Always 0 (success).
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	(void)argc;
	return (0);
}
