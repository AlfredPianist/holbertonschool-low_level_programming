#include "holberton.h"

/**
 * main - entry point
 *
 * Description: prints the character "_" n times,
 *              followed by a new line.
 * Return: Always 0.
 */
int main(void)
{
	print_line(0);
	print_line(2);
	print_line(10);
	print_line(-4);
	return (0);
}
