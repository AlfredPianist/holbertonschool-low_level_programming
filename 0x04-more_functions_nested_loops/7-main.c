#include "holberton.h"

/**
 * main - entry point
 *
 * Description: prints the character "\" n times,
 *              followed by a new line in a diagonal.
 * Return: Always 0.
 */
int main(void)
{
	print_diagonal(0);
	print_diagonal(2);
	print_diagonal(10);
	print_diagonal(-4);
	return (0);
}
