#include "holberton.h"

/**
 * main - entry point
 *
 * Description: prints the character "#" in a squared triangle shape
 *              of n lines, followed by a new line.
 * Return: Always 0.
 */
int main(void)
{
	print_triangle(2);
	print_triangle(10);
	print_triangle(1);
	print_triangle(0);
	return (0);
}
