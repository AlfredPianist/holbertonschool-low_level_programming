#include "holberton.h"

/**
 * mul - muiltiplies two numbers
 * @a: first factor.
 * @b: second factor.
 *
 * Return: the multiplication of integers a and b.
 */
int mul(int a, int b)
{
	int prod;

	prod = a * b;

	return (prod);
}
